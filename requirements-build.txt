# Build-time Python dependencies for library-articles.
#
# These are only needed when producing the static site (running build.py
# with BUILD_PDFS=1 or --pdf, and/or running jsx_to_markdown.py). At serve
# time the Space ships plain static files — no Python runtime needed.
#
# Local install:
#   pip install -r requirements-build.txt
#   playwright install chromium
#
# CI install: see .github/workflows/publish-to-hf.yml (installs the same
# requirements, then `playwright install --with-deps chromium`).

playwright==1.44.0
PyYAML
